Project Planning:

Two ways to move a div around on the page
	-Div is a block, so can use absolute, relative positioning, float, positioning with inline-block
Stick a div onto the bottom or top of the page
	positioning, fixed or absolute
Identify the background color of an existing webpage
	-dev tools - color in styles
Grab the URL for an image from an existing webpage
	-dev tools
Center an element horizontally
	-use margin: auto;
Identify three ways you can include your CSS styles in a page
	-inline, in heade, css file
Understand how to use classes and ids to target CSS at specific elements on the page
	classes have . in style, class="" in html
	ids have # in style, id="" in html
Build a very basic form (even if it doesn’t “go” anywhere)